{
  "name": "sdk-heaven-functions",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "admin:claim": "node scripts/setAdminClaim.js",
    "lint": "node --check index.js && node --check livekitTokenCore.js && node --check roomCommandCore.js && node --check adminClaimsCore.js && node --check adminDashboardCore.js && node --check scripts/setAdminClaim.js",
    "serve": "npx firebase-tools emulators:start --only functions",
    "deploy": "npx firebase-tools deploy --only functions",
    "logs": "npx firebase-tools functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.6.0",
    "livekit-server-sdk": "^2.13.3"
  }
}
