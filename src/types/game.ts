export type FeaturedGame = {
  id: string;
  title: string;
  provider: string;
  category: string;
  description: string;
  accentColor: string;
  tableLabel: string;
  playerCount: string;
  isComingSoon?: boolean;
};
